# pubspec.yaml
name: mobile_app
description: A new Flutter project for the Infinity-Booking customer app.
publish_to: 'none'

environment:
  sdk: '^3.9.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  image_picker: ^1.0.4 # For profile photo upload
  url_launcher: ^6.2.1 # For opening external links (e.g., contact)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/default_avatar.png # Add default avatar asset if needed later