// lib/screens/bookings_screen.dart

import 'package:flutter/material.dart';

class BookingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Bookings Screen Placeholder'),
      ),
    );
  }
}
